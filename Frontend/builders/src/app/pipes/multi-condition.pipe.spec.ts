import { MultiConditionPipe } from './multi-condition.pipe';

describe('MultiConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
